<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Pokedex!{% endblock %}</title>
        <link rel="icon" href="{{ asset('images/favicon.ico') }}" type="image/x-icon">
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.9.1/font/bootstrap-icons.min.css">
        {% endblock %}
    </head>
    
    <body>

        <header class="container">
            <nav id="navbar" class="navbar navbar-expand-lg border rounded-pill p-3 m-3">
                <div class="container-fluid flex-nowrap">
                    <a id="title" class="navbar-brand col-3" href="{{ path('home') }}">Pokedex</a>
                    <div class="navbar col-6" id="navbarSupportedContent">
                        <form id="search-form" class="d-flex col-12" method="get" action="{{ path('home') }}">
                            <input type="search" id="pokemon-search" name="query" class="form-control" placeholder="Recherche ..." value="{{ app.request.query.get('query') }}">
                        </form>
                    </div>
                    <div class="form-check form-switch d-flex justify-content-end p-0 col-3">
                        <a href="{{ path('pokemon_new') }}" id="add" class="btn px-2">
                            <i class="bi bi-plus-square"></i>
                        </a>
                        <button id="theme-switch" class="btn px-2" type="button" aria-label="Changer le thème">
                            <i class="bi bi-moon text-white" id="theme-icon"></i>
                        </button>
                    </div>
                </div>
            </nav>
        </header>

        <main>

            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="container alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}
            {% endblock %}
        </main>

        <footer>
            <p class="text-center mt-3">&copy; 2024 - Pokédex by <a href='https://francoismorel73.github.io/' target="_blank" rel="noopener noreferrer">François MOREL</a></p>
        </footer>

    </body>

    {% block javascripts %}
    <script src="{{asset('js/scripts.js')}}"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    {% endblock %}

</html>
